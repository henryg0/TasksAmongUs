import React from 'react';
import Card from '@material-ui/core/Card';

export default function Progress() {
  return (
    <Card
      variant="outlined"
      style={{
        overflow: "auto",
        maxHeight: "450px",
        height: "450px",
      }}
    >
      prog prog prog
    </Card>
  )
}